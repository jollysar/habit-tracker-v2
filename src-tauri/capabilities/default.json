{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege database access for Habitree's local main window",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-execute",
    "notification:default"
  ]
}
